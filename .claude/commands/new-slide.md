# Add a new slide to the presentation

Use the `slide-writer` agent to create a new slide.

Ask the user:
1. What is the concept this slide should communicate?
2. Where in the deck should it go — after which existing slide?

Then write the slide following the writing-slides skill, place it in the correct position, run `node build.js`, and confirm it was added.
